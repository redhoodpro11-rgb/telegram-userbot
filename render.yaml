services:
  - type: worker
    name: telegram-userbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: SESSION_STRING
        sync: false
